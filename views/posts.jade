extends layout

block content
	h1= title
	p Welcome to #{title}
	p meeeowww

	- var posts = listings? JSON.parse(listings) : [];

	each post in posts
		li 
			a(href='#{post.URL}') #{post.jobTitle} for #{post.company} (posted on #{post.source})